{
  "name": "blog.dotmavriq",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:release": "astro build && node scripts/cache-bust-css.js",
    "preview": "astro preview",
    "deploy": "npm run build:release && node scripts/gh-pages-publish.js && node scripts/site-live-publish.js",
    "test:parity": "node scripts/parity-check.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "@astrojs/mdx": "^3.1.0",
    "@astrojs/rss": "^4.0.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.15.0",
    "date-fns": "^4.1.0",
    "gray-matter": "^4.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.58.2",
    "typescript": "^5.5.0"
  }
}
