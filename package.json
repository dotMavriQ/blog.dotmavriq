{
  "name": "jonatan-devblog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "@astrojs/mdx": "^0.19.7",
    "astro": "^2.9.3"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
